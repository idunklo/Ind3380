#include <stdio.h>
#include <stdlib.h>

int main()
{  
    int n;
    n = 10;
    double *v = (double*)malloc(n*sizeof(double));

    for( int i=0; i<n; i++){
        v[i] = rand();

         printf("%d ", v[i]);   
    }

    free(v);

    return 0;
}

